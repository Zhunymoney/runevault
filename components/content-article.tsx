import type{ContentPost}from"@/lib/content-server";export function ContentArticle({post}: {post:ContentPost}){return <main className="mx-auto min-h-[760px] max-w-4xl px-6 py-16"><p className="text-sm font-black uppercase tracking-[.2em] text-amber-400">{post.type.replaceAll("_"," ")}</p><h1 className="mt-4 text-4xl font-black sm:text-6xl">{post.title}</h1><p className="mt-5 text-xl leading-8 text-white/50">{post.summary}</p><time className="mt-4 block text-sm text-white/30">{new Date(post.published_at).toLocaleDateString()}</time><article className="mt-10 space-y-5 rounded-3xl border border-white/10 bg-white/[.025] p-7 leading-8 text-white/65 sm:p-10">{post.body_markdown.split(/\n{2,}/).map((paragraph,index)=><p key={index} className="whitespace-pre-wrap">{paragraph}</p>)}</article></main>}
