import Link from"next/link";import type{ContentPost}from"@/lib/content-server";export function ContentList({posts,empty="No published content yet."}:{posts:ContentPost[];empty?:string}){if(!posts.length)return <p className="rounded-3xl border border-dashed border-white/10 p-10 text-white/40">{empty}</p>;return <div className="grid gap-4 sm:grid-cols-2">{posts.map(post=><article key={post.id} className="rounded-3xl border border-white/10 bg-white/[.025] p-7"><p className="text-xs font-black uppercase tracking-[.15em] text-amber-300">{post.type.replaceAll("_"," ")}</p><h2 className="mt-3 text-2xl font-black">{post.title}</h2><p className="mt-3 text-sm leading-6 text-white/45">{post.summary}</p><Link href={`/${post.type==="news"||post.type==="blog"?"news":"guides"}/${post.slug}`} className="mt-5 inline-block font-black text-amber-300">Read article →</Link></article>)}</div>}
