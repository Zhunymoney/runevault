import Link from "next/link";
const guides = [["Buying OSRS gold safely","Confirm your RuneScape name, use the exact order reference, and follow the delivery instructions shown in tracking."],["BTC payment checklist","Send only BTC to the displayed Bitcoin address, use the exact locked amount, and submit the transaction ID once."],["USDC on Base checklist","Send only USDC using the Base network. Other assets or networks may not be recoverable."],["Selling gold to RuneVault","Keep your payout and meetup details current, then follow the seller status from verification through payout."]];
export default function GuidesPage() { return <main className="mx-auto min-h-[720px] max-w-6xl px-6 py-16"><p className="text-sm font-black uppercase tracking-[.2em] text-amber-400">OSRS guides</p><h1 className="mt-4 text-5xl font-black">Clear marketplace help.</h1><section className="mt-10 grid gap-4 sm:grid-cols-2">{guides.map(([title,body])=><article key={title} className="rounded-3xl border border-white/10 bg-white/[.025] p-7"><h2 className="text-xl font-black">{title}</h2><p className="mt-3 text-sm leading-6 text-white/45">{body}</p></article>)}</section><Link href="/support" className="mt-8 inline-block font-black text-amber-300">Open complete help center →</Link></main>; }
