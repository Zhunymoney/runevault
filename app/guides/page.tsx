import Link from"next/link";import{ContentList}from"@/components/content-list";import{getPublishedContent}from"@/lib/content-server";const essentials=[["Buying OSRS gold safely","Confirm your RuneScape name, use the exact order reference, and follow tracking instructions."],["BTC payment checklist","Send only BTC to the displayed Bitcoin address and submit the transaction ID."],["USDC on Base checklist","Send only USDC using Base. Other assets or networks may not be recoverable."],["Selling gold to RuneVault","Keep payout and meetup details current through verification and payout."]];export default async function GuidesPage(){const posts=[...(await getPublishedContent({type:"guide"})),...(await getPublishedContent({type:"quest_helper"}))];return <main className="mx-auto min-h-[720px] max-w-6xl px-6 py-16"><p className="text-sm font-black uppercase tracking-[.2em] text-amber-400">OSRS guides</p><h1 className="mt-4 text-5xl font-black">Clear marketplace help.</h1><section className="mt-10 grid gap-4 sm:grid-cols-2">{essentials.map(([title,body])=><article key={title} className="rounded-3xl border border-white/10 bg-white/[.025] p-7"><h2 className="text-xl font-black">{title}</h2><p className="mt-3 text-sm leading-6 text-white/45">{body}</p></article>)}</section><section className="mt-10"><h2 className="mb-5 text-3xl font-black">Published guides</h2><ContentList posts={posts}/></section><Link href="/learn" className="mt-8 inline-block font-black text-amber-300">Search all learning content →</Link></main>}
