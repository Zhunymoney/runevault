import{notFound}from"next/navigation";import{ContentArticle}from"@/components/content-article";import{getPublishedContent}from"@/lib/content-server";export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const post=(await getPublishedContent({slug,limit:1}))[0];if(!post||!new Set(["guide","quest_helper"]).has(post.type))notFound();return <ContentArticle post={post}/>}
