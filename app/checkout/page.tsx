export default function CheckoutPage(){return <main className="mx-auto min-h-[700px] max-w-3xl px-6 py-16"><p className="text-sm font-black uppercase tracking-[.2em] text-amber-400">Payment integration</p><h1 className="mt-3 text-4xl font-black">Checkout is intentionally disabled.</h1><p className="mt-5 leading-8 text-white/50">The project includes a test-order workflow, but no live processor credentials or payment collection. Add a processor only after account approval, legal review, webhook verification, fraud controls, and refund handling are complete.</p></main>}
