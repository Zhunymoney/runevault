import{ContentList}from"@/components/content-list";import{getPublishedContent}from"@/lib/content-server";export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const{q=""}=await searchParams;const posts=await getPublishedContent({search:q,limit:50});return <main className="mx-auto min-h-[720px] max-w-6xl px-6 py-16"><p className="text-sm font-black uppercase tracking-[.2em] text-amber-400">RuneVault learning center</p><h1 className="mt-4 text-5xl font-black">Search OSRS marketplace help.</h1><form className="mt-8 flex gap-3"><input name="q" defaultValue={q} maxLength={100} placeholder="Search guides, news, and quest helpers" className="min-h-14 flex-1 rounded-xl border border-white/10 bg-white/[.025] px-4"/><button className="rounded-xl bg-amber-400 px-6 font-black text-black">Search</button></form><section className="mt-8"><ContentList posts={posts} empty={q?"No published articles match this search.":"No reviewed articles are published yet."}/></section></main>}
