import Link from "next/link";
export const metadata = { title: "Contact RuneVault" };
export default function ContactPage() { const email = process.env.NEXT_PUBLIC_SUPPORT_EMAIL?.trim(); return <main className="mx-auto min-h-[760px] max-w-4xl px-6 py-16"><p className="text-sm font-black uppercase tracking-[.2em] text-amber-400">Contact</p><h1 className="mt-3 text-4xl font-black">Get help from RuneVault.</h1><p className="mt-5 max-w-2xl leading-7 text-white/50">Use the authenticated support center for order, payment, delivery, refund, account, or marketplace questions. Never send passwords, wallet keys, or authentication codes.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><Link href="/support" className="rounded-2xl border border-amber-300/20 bg-amber-300/[.05] p-6 font-black text-amber-200">Open the support center</Link>{email ? <a href={`mailto:${email}`} className="rounded-2xl border border-white/10 p-6 font-black">Email {email}</a> : <div className="rounded-2xl border border-white/10 p-6 text-white/45">Email support is available after the verified support address is configured.</div>}</div></main>; }
