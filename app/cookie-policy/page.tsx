import { LegalPage } from "@/components/legal-page"; export const metadata={title:"Cookie Policy"}; export default function Page(){return <LegalPage title="Cookie policy" sections={[["Essential storage","RuneVault and Supabase use storage needed for authentication, refresh, payment continuity, and abuse prevention."],["Analytics","Analytics loads only when configured. Consent requirements must be reviewed before production analytics is enabled."],["Controls","Clearing or disabling essential storage may prevent sign-in, checkout, and private tracking."],["Retention","Document exact provider cookies, purposes, durations, and consent behavior after final configuration."]]}/>}
